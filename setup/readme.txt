
1) ini

sudo mkdir /etc/integration
sudo cp ../etc_integration_sync.ini.example /etc/integration/sync.ini
sudo chown -R <administrator user> /etc/integration
sudo chmod -R 750  /etc/integration


2) cert. for Windows Server: subdir cert